
// This file re-exports the component to resolve the module error
// while avoiding code duplication, as the main component lives in /components.
import LoginPage from './components/LoginPage';

export default LoginPage;
